import { Router } from "express";


const movieRouter = Router();

movieRouter.get("/", );
movieRouter.post("/", );
movieRouter.patch("/:userId", )
movieRouter.delete("/:userId", )

export default movieRouter;